# Как запустить приложение

Есть три варианта запуска в зависимости от ваших потребностей:

## 1. 🎵 `main.py` - Полное приложение (РЕКОМЕНДУЕТСЯ)

**Запустить:**
```bash
python main.py
```

**Что включено:**
- ✓ Веб-сервер (OBS overlay + donations feed)
- ✓ Monobank интеграция (проверка донатов)
- ✓ YouTube плеер (CLI консоль)
- ✓ **Красивое GUI приложение** (кнопки, слайдеры, очередь)

**Окна:**
```
1. Консоль (слева):
   [Main] Monobank integration enabled
   [Main] Polling for donations every 60 seconds
   [Input] Ready for test donations - press Enter

2. GUI приложение (справа):
   [Красивое окно с кнопками]
   ▶ Включить | ⏭ Следующий
   🔊 Громкость
   📋 Очередь треков
```

**Использование:**
- Нажимайте кнопки в GUI приложении
- Можете писать в консоль (Enter для тестового доната)
- Оба работают одновременно и синхронизированы

---

## 2. 💻 `base.py` - Только консоль (СЕРВЕРЫ/HEADLESS)

**Запустить:**
```bash
python base.py
```

**Что включено:**
- ✓ Веб-сервер (OBS overlay + donations feed)
- ✓ Monobank интеграция
- ✓ YouTube плеер (CLI консоль)
- ✗ Никакого GUI окна

**Использование:**
- Только текстовая консоль
- Подходит для запуска на серверах
- Подходит для headless режима
- Команды вводятся в консоль:
  ```
  [Player] > p     (Пауза)
  [Player] > n     (Следующий)
  [Player] > q     (Очередь)
  ```

---

## 3. 🎨 `player_gui.py` - Только GUI (ОТДЕЛЬНОЕ ПРИЛОЖЕНИЕ)

**Запустить:**
```bash
python player_gui.py
```

**Что включено:**
- ✗ Веб-сервер
- ✗ Monobank интеграция
- ✗ Консоль
- ✓ **Только красивое GUI приложение**

**Использование:**
- Запускайте отдельно
- Управляйте плеером через кнопки
- Синхронизируется с youtube_queue.json
- Полезно если хотите использовать плеер отдельно от основного приложения

---

## Сравнительная таблица

| Функция | main.py | base.py | player_gui.py |
|---------|---------|---------|---------------|
| **Веб-сервер** | ✓ | ✓ | ✗ |
| **OBS Overlay** | ✓ | ✓ | ✗ |
| **OBS Feed** | ✓ | ✓ | ✗ |
| **Monobank API** | ✓ | ✓ | ✗ |
| **Консоль плеера** | ✓ | ✓ | ✗ |
| **GUI приложение** | ✓ | ✗ | ✓ |
| **Работает одновременно** | CLI + GUI | - | - |
| **Синхронизация** | ✓ (одна очередь) | - | ✓ (одна очередь) |

---

## Рекомендуемые сценарии

### Сценарий 1: Стриминг с полным управлением
```bash
python main.py
```
Лучший выбор! Вы видите:
- Консоль с информацией о донатах
- GUI приложение для управления музыкой
- Оба работают вместе

### Сценарий 2: Запуск на сервере
```bash
python base.py
```
Нет GUI, только консоль. Идеально для:
- Linux серверов
- Headless режима
- Docker контейнеров
- Фонового процесса

### Сценарий 3: Только музыкальный плеер
```bash
python player_gui.py
```
Если хотите просто управлять плеером без основного приложения:
- Загрузите очередь из youtube_queue.json
- Воспроизводите и управляйте через GUI
- Идеально для автономного использования

### Сценарий 4: Несколько окон в разных местах
```bash
# Терминал 1:
python base.py

# Терминал 2 (или другой компьютер):
python player_gui.py
```
Запустите консоль и GUI отдельно, они синхронизируются через youtube_queue.json

---

## Требования

```bash
pip install -r requirements.txt
```

Зависимости:
- `aiohttp` - веб-сервер
- `pyyaml` - конфигурация
- `yt-dlp` - загрузка YouTube
- `pygame` - аудиоплеер
- `PyQt5` - GUI (только для main.py и player_gui.py)

---

## Горячие клавиши

### В main.py (консоль)
```
Enter - Отправить тестовый донат
```

### В base.py (консоль плеера)
```
p     - Пауза / Включить
n     - Следующий трек
v+    - Громче
v-    - Тише
q     - Показать очередь
c     - Текущий трек
s     - Выход
?     - Помощь
```

### В main.py и player_gui.py (GUI)
```
Щелчок по кнопке ▶ - Пауза / Включить
Щелчок по кнопке ⏭ - Следующий трек
Движение слайдера 🔊 - Регулировка громкости
```

---

## Советы

1. **Используйте main.py по умолчанию** - это самый полнофункциональный вариант
2. **Для серверов используйте base.py** - без GUI означает меньше использования памяти
3. **player_gui.py запускайте отдельно**, если нужен плеер отдельно
4. Все три варианта используют **одну очередь** (`youtube_queue.json`)
5. Закрывайте приложение через `Ctrl+C` в консоли

---

## Решение проблем

**GUI не запускается:**
```bash
pip install PyQt5
```

**Нет звука:**
- Проверьте что установлен pygame: `pip install pygame`
- Проверьте что видео < 10 минут
- Проверьте что mp3 файлы в `./youtube_cache/`

**Донаты не приходят:**
- Проверьте token в config.yaml
- Проверьте jar_id
- Смотрите логи в консоли (ищите "[DonationPoller]")

**Очередь пуста в GUI:**
- Добавьте через основное приложение (main.py) с донатом
- Или отредактируйте `youtube_queue.json` вручную
