# YouTube Player GUI

Отдельное GUI приложение для управления YouTube плеером с красивым интерфейсом на PyQt5.

## Запуск

```bash
python player_gui.py
```

## Интерфейс

### Основное окно

**Текущий трек:**
- Название трека
- Прогресс-бар воспроизведения
- Время текущей позиции и общая длительность

**Кнопки управления:**
- ▶ **Включить** / ⏸ **Пауза** - Управление воспроизведением
- ⏭ **Следующий** - Пропустить трек

**Регулировка громкости:**
- Слайдер от 0% до 100%
- Отображение текущей громкости

**Очередь:**
- Список всех треков в очереди
- Статус загрузки (✓ загружен, ⏳ загружается)
- Длительность каждого трека
- Выделение текущего трека

**Статус-бар:**
- Сообщения о событиях

## Функциональность

### Автоматическая загрузка из донатов

Когда приходит донат с YouTube ссылкой в комментарии:

```
"Слушайте это! https://youtu.be/dQw4w9WgXcQ"
```

Плеер автоматически:
1. Парсит URL из комментария
2. Проверяет длину видео (макс 10 минут)
3. Добавляет в очередь
4. Скачивает в фоне
5. Начинает воспроизведение при нажатии "Включить"

### Управление очередью

- Видны все треки в очереди
- Индикатор загрузки (✓ загружен, ⏳ ещё загружается)
- Длительность каждого трека
- Автоматическое удаление трека после прослушивания

### Кеш

- Максимум 10 видео в `./youtube_cache/`
- Старые файлы удаляются автоматически
- MP3 формат, 192kbps

### Очередь

- Сохраняется в `youtube_queue.json`
- Загружается при запуске приложения
- Автоматически обновляется при добавлении новых треков

## Требования

```bash
pip install PyQt5 yt-dlp pygame aiohttp pyyaml
```

Или установить все зависимости:

```bash
pip install -r requirements.txt
```

## Структура окна

```
┌─────────────────────────────────────┐
│     🎵 YouTube Player               │
├─────────────────────────────────────┤
│                                     │
│  Song Name - Artist                 │
│  ████████████░░░░░░░░░░░░░░ 50%    │
│  2:30 / 5:00                       │
│                                     │
│  ▶ Включить    ⏭ Следующий        │
│                                     │
│  🔊 ███████░░░░░░░░░░░░░░ 70%      │
│                                     │
│  📋 Очередь                         │
│  1. ✓ Трек 1 (4:20)                │
│  2. ⏳ Трек 2 (3:50)               │
│  3. ✓ Трек 3 (5:10)                │
│                                     │
│  Статус: Готово                    │
└─────────────────────────────────────┘
```

## Интеграция с основным приложением

Когда запущено основное приложение (`python main.py`):

1. **Основное приложение:**
   - Принимает донаты от Monobank
   - Парсит YouTube URL из комментариев
   - Автоматически добавляет в очередь

2. **GUI приложение:**
   - Показывает красивый интерфейс
   - Управляет воспроизведением
   - Показывает очередь

Оба приложения работают **независимо** и используют **одну и ту же очередь** (`youtube_queue.json`).

## Примеры использования

### Пример 1: Запуск основного приложения и GUI

```bash
# Терминал 1 - основное приложение
python main.py

# Терминал 2 - GUI приложение
python player_gui.py
```

Теперь при каждом донате с YouTube ссылкой:
- Основное приложение автоматически добавляет в очередь
- GUI приложение показывает трек в списке
- Можно управлять воспроизведением через интерфейс

### Пример 2: Только GUI приложение

Если ты просто хочешь управлять плеером:

```bash
python player_gui.py
```

Приложение загружает очередь из `youtube_queue.json` и позволяет управлять воспроизведением.

## Горячие клавиши

Будут добавлены в будущем.

## Настройка

GUI автоматически читает параметры из:
- `config.yaml` - конфигурация приложения
- `youtube_queue.json` - список треков

Нет дополнительных файлов конфигурации для GUI.
